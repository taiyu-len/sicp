;eva lu ator and louis reasoner are each experimenting with the metacircular
;evaluator. eva types in the definition of map, and runs some test programs that
;use it. they work fine. louis, in constrast, has installed the system version
;of map as a primitive for the metacircular evaluatro, when he first tries it,
;things go terribly wrong. explain why louis's map fails even though evas works.

;what
;
;
