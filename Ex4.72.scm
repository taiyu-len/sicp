;why do disjoin and stream-flatmap interleave the streams rather than simply
;append them? give examles that illustrate why interleaving works better.

;in the case of an infinite stream s1, it would never touch s2.
