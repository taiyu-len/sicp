;suppose that we implement test-and-set! using an ordinary procedure as shown in
;the text, without attempting to make the operation atomic. draw a timing
;diagram like the one in 3.29 to demonstrate how the mutex implementation can
;fail by allowing two processes to acquire the mutex at the same time

;THESE QUESTIONS ARE SO SHIT. WHY THE FUCK. it just fails, cause it does f
