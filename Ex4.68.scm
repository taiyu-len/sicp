;define  rules to implement th reverse operation of 2.18, which returns a list
;containing the same elements as a given list in reverse order. (hint: use
;append-to-form) can your rules answer both (reverse (1 2 3) ?x) and (reverse ?x
;(1 2 3))

;SEE 4.4 list rules sectionms
