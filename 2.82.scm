;show how to generzlize apply-generic to handle coercion in the general case of
;multiple arguments. one stratagy is to attempt to coerce all the arguments to
;the type of the first, then the second and so on. give an example of a
;situation where this stratagy is no sufficientyl general.

;where it has to keep rising. a^b^c^d or where conversion isnt possible

